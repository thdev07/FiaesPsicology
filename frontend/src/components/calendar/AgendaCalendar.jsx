// TODO: instalar e integrar @fullcalendar/react com drag-and-drop
// TODO: aplicar cores por status: confirmado=verde, pendente=amarelo, cancelado=vermelho

export default function AgendaCalendar({ appointments = [] }) {
  return (
    <div>
      <p>Calendário de agendamentos — implementação pendente (Fase 3)</p>
    </div>
  );
}
